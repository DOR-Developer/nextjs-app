import { Inter } from "next/font/google";
import { Open_Sans } from "next/font/google";

// export const globalfont = Inter({ subsets: ['latin']});
export const globalfont = Open_Sans({ subsets: ['latin']})